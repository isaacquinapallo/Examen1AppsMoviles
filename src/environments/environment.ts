// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.



export const environment = {
  production: false,
  firebase: {
    apiKey: "CREATE_YOUR_OWN_API_KEY",
    authDomain: "CREATE_YOUR_OWN_AUTH_DOMAIN",
    projectId: "CREATE_YOUR_OWN_PROJECT_ID",
    storageBucket: "CREATE_YOUR_OWN_STORAGE_BUCKET",
    messagingSenderId: "CREATE_YOUR_OWN_MESSAGING_SENDER_ID",
    appId: "CREATE_YOUR_OWN_APP_ID",
    measurementId: "CREATE_YOUR_OWN_MEASUREMENT_ID"
  }
};



/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
